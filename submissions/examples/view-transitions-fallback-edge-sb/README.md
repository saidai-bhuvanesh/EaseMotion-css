# View Transitions Fallback Support Detection (edge case)

## Overview

This submission addresses issue #82051 by providing a self-contained demonstration and reusable artifact for **View Transitions Fallback Support Detection (edge case)**, packaged inside `submissions/examples/view-transitions-fallback-edge-sb/` to comply with the repository contribution guidelines.

## Files

- `demo.html`
- `style.css`
- `README.md`
- `view-transitions-fallback-edge-sb.test.js`

## Usage

1. Open `demo.html` directly in a browser (no server needed).
2. Observe the effect and inspect `style.css` for the raw CSS.
3. Run `npx vitest run submissions/examples/<folder>/<file>.test.js` to execute the unit test.

## Why is this useful?

It demonstrates the concept in an isolated, reviewable form that fits the EaseMotion CSS philosophy: lightweight, dependency-free, and open to maintainer standardization before integration.

_Note: This PR was created by an AI agent (OpenHands) on behalf of saidai-bhuvanesh._
